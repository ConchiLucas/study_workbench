# 执行记录

## 2026-07-18

- 已确认用户要求：词库单词列表直接按单词关联最佳例句表。
- 已确认页面、`word_clean`、`word_clean_best_sentence` 数量均为 22,415。
- 已确认原始 `word` 表不能建立全局单词唯一索引。
- 当前进入设计说明阶段。
- 第一次全量差异查询运行结束但未返回输出，改用无缓冲输出和哈希连接聚合重新查询，不重复原方案。
- 已完成精确核验：两张去重表单词完全一致；原始 `word` 有 295 行大小写差异，没有首尾空格差异。
- 已将设计从 `lower(btrim(word))` 修订为 `lower(word)`。
- 进一步语义核验发现 `lower(word)` 会把大小写敏感的不同词义错误合并，不能作为最终关联键。
- 已使用精确 id、原值和含义守卫，将 `word.id=78494` 从 `april` 修正为 `April`；修改前后含义均为“四月”，直接等号关联验证成功。
- 已事务修正 44 条释义完全一致的大小写数据。
- 已人工核验并事务修正 120 条同义且无同词库冲突的数据，19 条同词库冲突项跳过。
- 已删除 `Metro`、`right`、`thanksgiving` 3 条错误最佳例句及级联 TTS 任务；基础词和候选句保留。
- 最终接口设计改为区分大小写的直接等号关联。
- CSV 审计导出时第一次查询因 PostgreSQL `SELECT DISTINCT` 的排序表达式限制失败，已改用 `GROUP BY word` 查询。
- 已通过事务版本号精确还原 165 条修改记录，并加入 3 条最佳例句删除记录；CSV 共 168 条数据、169 行（含表头），已通过表格导入和渲染校验。
- 已按精确大小写从 `word` 删除 `Metro/right/thanksgiving` 共 23 行，从 `word_clean` 删除 3 行，级联清理 15 条候选句和 15 条旧造句任务。
- 最终完整性校验：`word=152986`、`word_clean=22412`、`word_clean_best_sentence=22412`，最佳例句缺失和孤儿均为0。
- 已开始全量核验 22,412 条最佳例句的目标词使用一致性，并完成词形/大小写初步分类。
- 低分异常词首轮删除被事务前数量守卫拦截：实际原词26行而非预估27行，未执行任何删除；随后按实时数量修正。
- 数量守卫修正后已删除 14 个与正确词条冲突或无法判定的低分词条，并从原 `word` 表删除 26 行精确拼写记录。
- 已使用带数量守卫的事务同步修正 `loder->loader`、`cando->condo`、`Lowa->Iowa`、`pleasureable->pleasurable`：原表 4 行、基础表 4 行、候选句 20 行、造句任务 20 行、最佳例句 4 行；删除 4 条旧 TTS 任务。
- 已低分复核 45 条最佳例句，删除语义不一致的 `secureness` 最佳例句和 1 条 TTS 任务，保留 5 条候选句。
- 用户要求 `word_clean_best_sentence` 也保留记录：已使用与任务中心相同的排序规则，从现有候选句恢复 `approaching/lumiere/macdonald/maker/secureness/socks/yard` 7 条最佳例句记录，加上“待重新造句”标记并将 TTS 置为 `pending`。
- 最终验证：`word_clean=22375`、`word_clean_best_sentence=22375`；重复、孤儿、单词差异、释义差异、来源句差异均为 0；7 条待重新造句记录已保留。
- 全量词法扫描复核通过：剩余 25 条非直接命中均为合法的复合词、所有格或词形变化。
- 已为 7 个待处理词人工生成新最佳例句，并同步修正 `macdonald->McDonald's` 和 `secureness` 错误释义。
- 小米 TTS 首次批处理在进入网络请求前因异步入口写法错误被拦截，未生成文件且未写库；修正后 7 条全部生成成功。
- 首次 URL 按现有新记录惯例写为 `19186/api/tts/files`，验证发现本批文件实际由 word-agent `8010/v1/tts/files` 提供；已修正最佳句和任务表共 14 个 URL 字段。
- 最终验证：7 条最佳句和 7 条 TTS 任务均为 `success`，所有 URL 返回 `HTTP 200 audio/wav`，文件大小和 WAV 时长一致，全局最佳句关联差异为 0。
- TDD 红灯：新增 Go 测试后，旧实现因缺少精确关联、最佳例句投影和原始单词索引初始化函数而编译失败，符合预期。
- TDD 绿灯：实现 `wcbs.word = w.word` 关联、例句/翻译替换、关键词搜索替换与两个唯一索引初始化，目标测试通过。
- 已在实时数据库创建 `uk_word_clean_word` 和 `uk_word_clean_best_sentence_word`；索引状态均为 unique/valid/ready，精确关联数为 22,375。
- 已运行 `go test ./...`，全部通过；原始词库列表 152,933 行，左关联后仍为 152,933 行，抽样返回例句与最佳例句表完全一致。
- 例句播放功能 TDD 红灯：旧 `Words` 响应未返回最佳例句 TTS 字段，新测试按预期失败。
- 已补充 Go TTS 响应字段、React 单实例播放器、播放/停止/加载/失败状态以及 Vite MinIO 代理。
- 已重启 8009 Go 服务加载新字段，真实 UI 验证普通音频和 MinIO `abase` 音频均能播放，播放结束自动复位，无控制台错误。
- 最终 `go test ./...`、`npm run build` 和 `git diff --check` 均通过。
- TTS MinIO 迁移前核验：22,363 条成功记录中 43 条已在 MinIO、22,313 条来自任务中心磁盘、7 条来自 word-agent 磁盘；零缺失、零大小差异。
- 已新增可断点续跑脚本 `word_select_dashboard/word-agent/scripts/migrate_best_sentence_tts_to_minio.py`，使用当前 URL 精确选择源目录，上传后校验 MD5 和大小，每 250 条提交数据库。
- 已先试迁移 10 条并通过对象/数据库/代理播放验证，随后完成剩余 22,310 条，全过程无上传或数据库守卫失败。
- 最终全量验证：MinIO/DB 均为 22,363 对象、5,831,816,612 字节，missing/extra/size mismatch/field mismatch/local URL 均为 0；7 个跨区间播放样本全部成功。
## 2026-07-18 TTS 本地文件清理

- 删除前从全量成功记录中均匀抽样 20 条，经 Go 播放代理读取 MinIO：20/20 返回 HTTP 200、`audio/wav`，且 RIFF/WAVE 文件头有效。
- 已删除以下两个目录中的全部 `.wav` 文件，目录本身保留，未删除其他文件：
  - `/Users/conchi/workforce/python_workforce/ai-task-center/data/tts_audio`：22,412 个
  - `/Users/conchi/workforce/rob_english_word_workforce/word_select_dashboard/word-agent/tts_audio`：50 个
- 删除后两个目录的 WAV 数量均为 0，占用均为 0B，释放空间约 5.5 GiB。
- 删除后再次抽样 5 条，通过 Go 播放代理从 MinIO 读取：5/5 返回 HTTP 200 WAV。
- 数据库成功记录中非 MinIO 地址数量为 0。
## 2026-07-18 去重单词表播放按钮

- 在“去重单词表”的最佳例句右侧增加播放/暂停按钮。
- 仅当最佳例句 TTS 状态为 `success` 且 MinIO URL 非空时启用。
- 支持单实例播放、重复点击停止、切换单词停止、播放结束复位、失败提示，以及筛选、翻页、刷新时停止。
- 新增播放 URL 纯函数测试：3/3 通过；React TypeScript 与 Vite 构建通过；`git diff --check` 通过。
- 实时接口抽样 `abase`：MinIO 代理返回 HTTP 200、`audio/wav`、199,724 字节。

## 2026-07-18 人名与地名清单

- 已确认分析范围为 `word_clean` 22,375 条，国家不计入地名。
- 已提取并检查 576 条大写专名候选，以及 258 条小写释义关键词候选。
- 当前进入保守分类和最佳例句复核阶段。
- 首次导出由存在性守卫拦截：`Ogallala/Scandinavia` 实际以小写存储；未输出不完整 CSV，已修正清单大小写。
- 已完成保守人工分类并补入漏检的 `Hampshire`。
- 已输出 `outputs/word_clean_person_place_names.csv`：279 行（地名 142、人名 137）。
- 最终 QA：分类内无重复、数据库逐行差异 0、国家关键词命中 0、最佳例句空值 0。

## 2026-07-18 全库删除人名与地名

- 用户确认删除 CSV 中全部人名和非国家地名。
- 删除范围为 279 行分类、276 个唯一单词；当前进入依赖盘点和事务 dry-run。
- 首次 dry-run 在词库计数步骤因列名 `word_library_id` 不存在而回滚，未删除或修改任何数据。
- 修正列名为 `word.library_id` 后，dry-run 精确锁定：`word` 486 行、`word_clean` 276 行、候选句 1,380 行、造句任务 1,380 行、TTS 任务 274 行、最佳例句 276 行及少量练习记录。
- 已先生成压缩行级备份和逐词审计 CSV，再在单事务中完成删除；53 个受影响词库的 `word_count` 已按实际行数重算。
- 删除后独立连接复核所有相关单词字段、JSON 答案字段和旧 ID 引用，残留均为 0；候选句、最佳例句、造句任务、TTS 任务孤儿均为 0。
- 当前 `word_clean=22,099`、`word_clean_best_sentence=22,099`、候选句和造句任务均为 110,495、TTS 任务为 22,093。
- 国家样本 `Canada/China/France/Germany/Italy/Japan` 均保留；MinIO 音频对象不属于数据库表，本次未删除。
- 发现 4 个未受本次删除影响的历史词库计数差异（ID 4/9/20/68）；本次涉及的 53 个词库无计数差异。

## 2026-07-18 三词挖空题 TTS 与 MinIO

- 已确认旧链路只生成句子并由 Java 挖空落库，`sentence_audio_url` 未写入；独立 TTS 接口也只保存本地文件。
- word-agent 造句接口现按 LLM -> 小米 MiMo TTS -> MinIO 顺序执行，MinIO object key 前缀为 `sentence_cloze_tts/`，响应返回 URL、bucket、key、大小、内容类型及 TTS 模型信息。
- TTS 或 MinIO 任一步失败均返回失败；临时 WAV 在 MinIO 成功或失败后都会删除，不长期保存在本地。
- Java 只有在收到非空 `sentenceAudioUrl` 后才插入 `sentence_cloze_item`；失败时现有错词策略会把该批 3 条事件恢复为 `pending`。
- 将外层错词批次 HTTP 超时从 90 秒提高为 300 秒，覆盖默认 180 秒 LLM、60 秒 TTS 和上传余量。
- word-agent 测试 17/17 通过；本次涉及 Python 文件 Ruff 通过。Java 全套测试 28/28 通过，`git diff --check` 通过。
- 已重启 word-agent 8010 和 Java 后端 8019/9091；word-agent 健康检查正常，MinIO `ai-file-navigation` bucket 可访问，新前缀和 300 秒超时配置已加载。
- 为避免消耗付费模型额度，本次运行态验证没有真实生成新句子或 TTS；LLM、TTS、MinIO 成功与失败路径由隔离测试覆盖。

## 2026-07-19 按 AGENTS.md 启动项目

- 已读取根 `AGENTS.md`，开始按 ctx 文档链路确认需要启动的子项目和服务依赖。
- ctx 使用规则接口连接失败，且本机未发现 ctx CLI/会话变量；已记录此阻塞，转为检查仓库启动脚本，继续推进服务启动。
- 已确认全部 6 个应用服务当前停止，数据库、Redis、MinIO 依赖正常运行。
- 首次执行统一重启入口因系统 PATH 缺少 Maven 中止；已定位可用的 IntelliJ Maven，下一步以临时 PATH 重试，不修改项目配置。
- 已用 IntelliJ Maven 临时补充 PATH，统一重启脚本执行成功；word-agent、Go API、Java API/WebSocket 和三个 Vite 前端全部启动。
- 已独立验证 6 个 launchctl 进程均为 RUNNING，端口 7001/7002/7003/8009/8010/8019/9091 全部可用，三个页面和 word-agent 健康接口返回 200。
- 最终新鲜验收通过：6 个应用进程仍为 RUNNING；三个前端、Go 业务接口和 word-agent 健康接口均返回 200；Java 受保护健康端点返回预期 403；WebSocket 9091 TCP 连接成功。

## 2026-07-19 难度选择关闭层级修复

- 已复核截图对应的 `HomeView` 两层状态与关闭函数，确认第一次只关闭难度选择层、第二次才关闭训练设置层，是可稳定复现的状态逻辑问题。
- 尚未修改生产代码；按设计确认与 TDD 流程，等待确认叉号和选中难度的行为边界。
- 用户确认采用“一次关闭回主页”的方案并要求审计其他页面；已完成全量关闭链路审计。
- 已新增修复设计文档，范围锁定为难度选择、今日答题结果和已掌握单词三条训练工作流；生产代码仍未修改。
- 已添加关闭流程回归测试；首次定向运行 5 项中 2 项按预期失败，分别证明 Home 仍无统一退出函数、结果和已掌握页面仍会重开训练设置。
- 已实施最小修复：Home 两个叉号与 Escape 统一清除两个训练层状态；结果页和已掌握页关闭直接导航 `/home`。定向测试现为 5/5 通过。
- 全量前端测试 13/13 通过，`npm run build` 成功，`git diff --check` 无错误；生产 diff 仅涉及 3 个视图和 1 个回归测试。
- 已尝试 7002 运行态点击验收，但独立浏览器会话停在登录页；已将该登录页显示并保留，等待用户登录后继续验证。

## 2026-07-19 单人训练按钮文案

- 已定位 `HomeView` 按钮默认文案和现有操作顺序测试；先将测试期望改为“单人训练”，生产文案尚未修改。
- 定向测试先因找不到“单人训练”按预期失败；将按钮默认文案替换后，定向测试 3/3 通过。
- 完整前端测试 13/13 通过，生产构建成功，`git diff --check` 无错误；源码核对确认按钮默认态为“单人训练”、等待态仍为“训练准备中...”。

## 2026-07-19 关闭层级校正

- 已按用户反馈把回归契约改为“只回直接上一级”；旧实现定向测试 5 项中 2 项按预期失败。
- 已拆分难度训练与难度选择的关闭函数，并恢复答题结果、已掌握单词返回难度训练的路由状态。
- 修复后定向关闭契约测试 5/5 通过；完整前端测试 13/13 通过，生产构建成功，`git diff --check` 无错误。
- 用户最新确认难度选择显式关闭应一次回主页；已更新最终设计说明，尚未修改生产代码，等待书面设计复核后进入新一轮 TDD。
- 已按最终确认完成新一轮 TDD：旧实现的定向契约测试 5 项中 1 项按预期失败，证明叉号仍只关闭选择层；最小修改后定向测试 5/5 通过。
- 难度选择叉号和 Escape 现同时清除 `showDifficultyPicker` 与 `showTrainingSetup`；选择具体难度的两个处理函数未改变，仍只关闭选择层。
- 完整前端测试 13/13 通过，生产构建成功，`git diff --check` 无错误。
- 独立验收浏览器没有继承 Chrome 登录态，7002 被登录页拦截；未读取或请求用户凭据。
- 已新增完整挂载 `HomeView` 的 DOM 交互测试：叉号一次回主页、Escape 一次回主页、选择具体难度回训练设置，3/3 通过。
- 使用用户授权的 `conchi` 本地账号登录可见内置浏览器后，真实复现第一次点击只关闭底层训练页、选择层残留；确认是两个叉号的层叠顺序问题。
- 新增 z-index 回归契约，RED 明确显示 `90` 不大于 `3000`；将难度选择覆盖层提高到 4000 后 GREEN。
- 真实浏览器重新加载后，一次点击状态为 `home=true`、`training=false`、`picker=false`；选择“7年级上册”则为训练页可见、选择层不可见。
- 按用户最终反馈把难度选择叉号/Escape 恢复为只关闭选择层；测试先出现 3 个预期失败，最小修改后定向测试 9/9 通过。
- 登录后的内置浏览器重新加载验证：一次点击后 `home=false`、`training=true`、`picker=false`，页面停留在“难度训练”。

## 2026-07-19 三条答题链路进入 7003 错题挖空分析

- 已追踪主页匹配、难度匹配、单人训练三个入口到 WebSocket、统一答题落库、错词通知、word-agent 三词组批和 7003 查询接口。
- 已用只读 PostgreSQL 查询核验当前账号：匹配答错 113 条、单人训练答错 42 条；现有 27 条游戏来源错词事件全部是功能上线后的单人训练事件，旧匹配错题没有回填。
- 已确认 7003 首页只显示“已在挖空练习中答错且到期”的复习计划；新生成的三词挖空题先进入待答题，不会因比赛中答错就立刻出现在首页到期错题数。
- 已发现当前 6 条 pending 事件，其中最老 3 条因生成接口 HTTP 400 反复阻塞后续批次；本轮仅分析，没有修改代码或数据。

## 2026-07-19 删除旧匹配错题

- 按账号 `conchi`、`game_record.mode='match'`、`game_answer_detail.is_correct=0` 精确锁定并删除 113 条旧匹配答错明细，涉及 34 场，时间范围 2026-03-14 至 2026-04-02。
- 删除前完整备份到 `outputs/old_match_wrong_answers_conchi_20260719.json`；113 行，70,383 字节，SHA-256 为 `053938f71262256a45a7c66f36e5a308d4c26bdb0afc6d0ad27fda5cdf8add67`。
- 最终复核：旧匹配答错为 0；匹配正确答案仍为 33；单人训练答错仍为 42；`wrong_word_events` 对删除 ID 的引用为 0；11 道 `word-agent` 挖空题未改变。

## 2026-07-20 挖空练习待练口径与旧题兼容

- 已完成只读根因核验：140 是全部来源未答题数，首页显示的是到期错题数，两者当前口径不同。
- 已确认 92 条旧来源题目被当前单独训练回退查询排除；生产代码尚未修改。
- 已确认本次不处理 Word Agent 模型接口凭证，当前处于设计确认阶段。
- 用户澄清单独训练不属于错词生成链路，本轮停止旧题兼容设计，改为只读诊断 Word Agent。
- 已确认实际错词生成失败 15 条，均由活动大模型接口 403 导致；另有 2 条等待第三个错词凑批。
- 已确认页面 140 不是错词生成队列，而是已经有完整句子的未答题目统计；本轮未修改代码、配置或数据。
## 2026-07-18 最佳例句挖空字段回填

- `word_clean_best_sentence` 新增 `cloze_sentence`、`cloze_answer` 两个非空文本字段。
- 实时数据库 22,375 条全部完成事务回填：原词匹配 21,470 条、变形词 879 条、词组锚点 26 条、重复位置全部挖空 26 条。
- 空字段 0、缺少 `____` 0、逐条重算不一致 0、无法匹配 0。
- 最佳例句评分 upsert 已同步计算挖空字段，后续 sentence 变化不会保留旧挖空内容。
- word-agent 测试 12/12 通过；本次 Python 文件 Ruff 检查通过；`git diff --check` 通过。

## 2026-07-20 Word Agent 本地 CLI 配置

- 已阅读 `ai-task-center` 的 CLI DTO、Java 配置服务、React 管理页面及 Python Worker CLI 执行器。
- 已核对目标项目 Go AI 配置接口、数据库模型、React 模型配置页和 Python `LLMClient`。
- 已确认要让 CLI 替代失效 API 完成错词造句，必须接入 Word Agent 的真实文本生成入口；当前等待用户确认运行时范围后再提交设计选项。
- 尚未修改生产代码、配置或数据库。
- 已核对当前 Docker 运行态：`word-agent:1.0.0` 运行于 Linux arm64，容器内没有 Codex CLI；正在确认采用容器内 CLI 还是宿主机执行桥接。
- 用户确认采用容器内 Codex CLI，并只接管错词造句；进入完整设计确认阶段。
- 按用户要求暂停 CLI 设计，已完成 Python Word Agent AI 功能只读盘点，准备按上线状态汇总。
- 已按 TDD 删除两项未使用能力：删除前定向测试 2 项按预期失败，删除后定向测试 3/3 通过；完整 Word Agent 测试 51/51 通过，Ruff 与 `git diff --check` 通过。

## 2026-07-20 MiMo TTS 统一模型配置

- 已盘点项目内所有 MiMo 生成路径，确认运行时集中于 `MiMoTTSService`，另有一个独立示例脚本直接调用 API。
- 已只读确认数据库现有 5 条文本模型配置及容器 MiMo 参数，尚未修改或删除配置数据。
- 当前处于设计确认阶段，生产代码、Docker 和数据库均未因本需求发生变化。
- 进一步核对发现 AI Task Center 也有实际 MiMo TTS 批处理能力；正在确认是否纳入统一配置范围。
- 用户确认 AI Task Center 也纳入改造；已完成跨项目调用链设计，推荐由 Word Agent 作为唯一 MiMo 调用网关，等待最终设计批准。
- 用户随后明确两个项目必须保持独立；已撤销跨项目方案，AI Task Center 不在本次修改范围内。
- 用户进一步修订为保留现有 5 条文本模型，并新增独立 TTS 模型配置；已撤销删除文本模型的数据迁移计划。
- 已完成独立 TTS 模型配置设计文档、自审并单独提交为 `a3aaba9`；等待用户复核文档后进入实施计划。
- 用户已批准独立 TTS 配置设计；已生成 11 个任务的 TDD 实施计划，覆盖 Go 管理 API、React 独立页面、Word Agent 数据库读取、示例脚本、一次性密钥迁移、回归与 Docker 验收。
- 实施计划再次锁定边界：不改 `ai-task-center`、不动现有 5 条文本模型、不输出 MiMo API Key；尚未开始生产代码、数据库或 Docker 变更。

## 2026-07-26 非 Docker 启动全部前后端项目

- 已读取根 `AGENTS.md` 和现有规划记录，开始按 ctx 文档链路重新确认当前全部子项目。
- 本次仅启动宿主机上的应用进程，不使用 Docker 启动或重建服务。
- ctx 使用规则接口连接失败，无法通过 ctx 读取链路文档；已按规则停止直接读取这些 Markdown，转为检查仓库脚本、配置和运行态。
- 已完成非 Docker 启动入口、工具链、依赖目录和端口盘点；全部应用当前停止，基础设施已可用。
- 统一启动第 1 次在 `word_agent` 的 `launchctl bootstrap` 阶段返回 I/O error 5；已停止继续尝试，先调查根因。
- 用相同 plist 在授权宿主机环境执行 bootstrap 成功，Word Agent 已成为 RUNNING；这证实原 I/O error 来自受限执行环境。
- Word Agent 当前实际监听 6017 而非脚本预期的 8010，正在定位配置来源后再继续。
- 已确认 HEAD 的最新提交将全项目端口迁移到 601x，而现有启动脚本仍是旧端口；将按当前源码配置逐项目启动和验证。
- 已完成当前端口映射和启动参数校正；准备在宿主机 launchd 中重启 6 个应用，关闭旧端口等待并让 Java 指向 Word Agent 6017。
- 已在宿主机 launchd 中成功启动全部 6 个应用项目；当前 7 个端口 6011–6017 全部监听，进入 HTTP、健康接口与 WebSocket 验收。
- 浏览器逐页验收完成：三个前端均真实渲染且无控制台错误。
- 最终独立检查通过：6/6 应用进程 RUNNING、7/7 端口可连接、HTTP/健康检查全部符合预期，验证失败数为 0。

## 2026-07-26 四个答题入口的错词造句队列核对

- 已恢复此前三条游戏答题链路的分析记录；本轮按用户四张截图补充核对挖空复习与挖空单独训练两个入口。
- 本轮仅做源码、运行日志和数据库只读核验，不修改业务实现或数据。
- 已逐张复核截图并固定四个入口口径；下一步从前端事件和 Java 提交接口开始追踪。
- ctx 规则服务仍不可用，已记录后按允许范围回退到源码、运行日志和只读数据库。
- 前端初步定位完成：两个匹配入口共用 `handleMatch`，两个挖空入口共用 `handleSubmitAnswer`；正在读取精确请求参数和后端落库逻辑。
- Java 入队条件核对完成：四个入口的答错都会调用 Word Agent 错词事件接口；两个游戏入口使用游戏来源，两个挖空入口使用挖空来源。
- Word Agent 组批状态机核对完成：事件先持久化，再按同一用户组批；成功、等待重试和最终失败均有独立状态。
- 当前队列表只看到游戏来源，未看到挖空来源；正在跨库核对游戏模式和挖空答错记录，避免把“无样本”误判为“不入队”。
- 跨库核验确认现有队列包含 match 与 solo_training 游戏事件；挖空库当前没有任何答错记录，因此没有挖空来源事件属于无测试样本。
- 难度字段复核完成：现有 match 队列样本均来自指定难度匹配；首页段位匹配虽然无现存样本，但与其共用完全相同的前后端答错通知链。
- 新鲜只读数据库复核完成：25 条游戏来源事件中 23 条 match、2 条 solo_training；状态为 24 failed、1 pending，挖空答案表仍为 1 条正确、0 条错误。
- Word Agent 定向测试 8/8 通过；Java 三个相关服务测试首次因沙箱禁止绑定回环端口出现 4 个环境错误，在授权宿主机环境以相同命令重跑后 15/15 通过。
- 四个入口结论和例外条件已汇总；本轮未修改业务代码、配置或数据库。

## 2026-07-26 failed 与 retry_wait 根因核对

- 已只读拆分 `conchi` 的 21 条异常状态事件：18 failed 为 6 个三词批次且均重试 3 次，3 retry_wait 为 1 个三词批次且已重试 2 次。
- 已确认历史 failed 分为 15 词上游模型 403 链路和 3 词网络不可达链路。
- 已从当前 Word Agent 堆栈定位最新 retry_wait：CLI 路径对 `None` DSN 调用 `.strip()`，尚未实际运行 Codex CLI。
- 已确认该回归来自当前 HEAD `61d4f72`，现有测试缺少默认 `select_db_dsn=None` 场景；本轮按诊断权限未修改或重试。
- 已确认 Codex CLI 二进制、版本和 ChatGPT 登录状态正常。
- 已用项目 `CLIRunner` 和合成词真实生成成功，确认 CLI、当前模型与结构化输出可用；未向外部服务发送用户错词。
- 已用相同合成词请求当前 Word Agent，得到 HTTP 500，并从新鲜日志复现 `select_db_dsn=None` 的 `.strip()` 异常。
- 已发现 6018 Runner 当前未运行，且 CLI 集成测试仍验证旧 HTTP Runner 架构；定向测试 1 项失败并复现生产异常。本轮仍未修改业务代码或队列。

## 2026-07-26 本地无鉴权 CLI Runner 双模式兼容

- 用户批准采用宿主机 6018 Runner 统一兼容 Docker/非 Docker，并明确暂不需要
  Token 鉴权。
- 已复核原统一执行器设计和当前回归，完成新的兼容设计文档；未修改生产代码、
  启动配置、服务或队列。
- 设计覆盖 HTTP 适配器、Runner 无鉴权边界、双模式 URL、启动顺序、错误处理、
  测试、运行态验收和历史队列隔离。
- 设计文档完成自审并单独提交为 `200aa23`；规划记录保持未暂存，等待用户复核
  设计文档后再进入实施计划。
- 已按 TDD 恢复 Word Agent 到 6018 的 HTTP 调用，原生和 Docker URL 共用同一
  代码路径；`select_db_dsn=None` 不再影响 CLI 生成。
- 已删除 Runner Token 设置、Bearer Header 校验、Token 文件和 Compose Token
  依赖；Runner 只要求数据库 DSN。
- 已把根非 Docker 启动脚本更新为当前 6011–6018 端口并纳入 CLI Runner；真实
  重启中发现 launchd 卸载竞态，完成三次有限重试修复后全服务启动成功。
- Word Agent 完整测试 168/168 通过；Compose 配置、zsh/bash 语法和
  `git diff --check` 通过。
- 使用合成词 `quartz/meadow/lantern` 经 6017→6018→Codex CLI 真实生成成功，
  当前模型为 `gpt-5.3-codex-spark`；错题表状态、整表摘要前后完全一致，合成词
  入队数为 0。

## 2026-07-26 可入队错题记录列表

- 用户已批准完整设计：用户端错题集统计普通和挖空的全部可入队错误单词事件，并改为七字段扁平列表。
- 用户最终确认所有用户端、管理后台错题/训练/造句相关表格都删除“你的答案”展示列。
- 设计文档已完成自审并提交为 `88fcb20`。
- 已恢复现有规划上下文，确认工作区中的上一项立即复习改动必须完整保留。
- 已盘点 Java mapper/controller、Vue 两个目标页面、React 六类渲染位置和对应 CSS 网格；进入 TDD 实施计划编写阶段。
- 已生成六任务 TDD 实施计划，覆盖 Java 统一事件 SQL/接口、Vue 错题集重构、两端全部答案列删除、完整测试构建和非 Docker 运行态验收。
- 实施计划自审确认没有占位内容；修正了规划文件中首次通用复选框替换未命中当前任务段的问题。
- Java SQL Provider 测试完成 RED：测试编译因 Provider 不存在失败，证明用例锁定了新增行为。
- 已添加统一事件 DTO、共享 PostgreSQL CTE Provider 和两个 Mapper 方法；旧聚合接口及查询未修改。
- SQL Provider 聚焦测试完成 GREEN：2/2 通过；开始锁定 `/api/wrong-words/events` 控制器契约。
- 控制器契约测试完成 RED：反射得到预期 `NoSuchMethodException`。
- 已新增 `/api/wrong-words/events`，复用现有搜索和排序归一化，分页上限与用户端已有 500 条选项一致；旧聚合和详情接口保留。
- Java 统一事件 SQL 与控制器聚焦测试完成 GREEN：3/3 通过；进入用户端错题集重构。
- 2026-07-26：`WrongWordsView.contract.test.ts` 已由 RED 转为 GREEN（2/2）；错题集已切换 `/api/wrong-words/events`，展示七列可入队事件并移除聚合详情弹窗。
- 2026-07-26：Vitest 仍报告沙箱禁止监听 `0.0.0.0:24678`（EPERM），但测试进程正常执行且断言全部通过；属于测试环境 HMR WebSocket 警告，不影响合同测试结果。
- 2026-07-26：用户端“你的答案”展示合同测试完成 RED→GREEN；`WrongWordsView` 与 `TrainingAnswerResultsView` 共 4/4 断言通过，训练明细表同步收缩为 7 列。
- 2026-07-26：后台答案列合同测试完成 RED→GREEN（4/4）；独立错题、用户详情错题、挖空错题、训练结果及造句来源展开表均删除“你的答案”，对应 CSS 网格各减少一列。
- 2026-07-26：两端生产构建均通过。用户端完整测试暴露旧导航合同仍要求已删除详情弹窗的第二个关闭按钮，按新页面契约调整；后台完整测试 72/73，通过项含本次合同，唯一失败为未改动模型配置组件的历史 `deleteCLIProvider` 期待与当前实现不一致。
- 2026-07-26：用户端完整测试修正后 25/25 通过；Java 完整测试沙箱内因临时端口绑定失败，按规则在宿主环境重跑后 60/60 全部通过。
- 2026-07-26：真实 PostgreSQL 只读执行生产 `EVENT_CTES` 成功；现存可入队记录按用户分别为 game 15、6、22 条，当前库没有 `is_correct=false` 的挖空答题记录，因此运行态样本暂时只有普通答题来源。
- 2026-07-26：非 Docker 重启完成，6011–6018 全部就绪；后台浏览器运行态展开验收确认仅显示正确答案、不显示“你的答案”。用户端浏览器因已退出登录未做可视化验收。
- 2026-07-26：日志首轮扫描命中持久文件中的旧进程 HTTP 500；按当前启动进程号重新核对后，21:47 新进程启动段无 ERROR。
- 2026-07-26：代码审查无 Critical，修复两项 Important：用户错题集在 `<1200px` 改用完整卡片布局；挖空词难度优先按 `source_word_ids_json` 同序来源 ID，失配时按英文回退。
- 2026-07-26：PostgreSQL 临时表执行级测试通过：4 条合成事件覆盖无来源记录普通错误、普通/挖空 UNION、数组长度不等拆词、来源词 ID 优先、英文回退及 count 排序；临时表随连接删除。
- 2026-07-26：补充控制器行为测试后 Java 聚焦 4/4、Vue 聚焦 10/10、用户端生产构建均通过。
- 2026-07-26：最终全量回归：Java 61/61、用户端 25/25、后台本任务合同 4/4、用户端与后台生产构建、`git diff --check` 全部通过。
- 2026-07-26：审查修正后再次完成非 Docker 全服务重启；6011–6018 全部 Ready，最终进程状态均 RUNNING，当前启动日志无 ERROR/Traceback。
# 2026-07-26 错题集持久复习状态口径修正

- 已确认上一版把错题集误建模为错误事件历史；本轮暂停继续实现，先回到复习状态口径。
- 已定位 `sentence_cloze_review_schedule` 的完成条件：连续完成三个复习阶段后删除计划记录；正在继续追踪队列事件与生成句子的关联方式。
- 已确认队列状态和复习状态不能混用；现有 `source_event_ids_json`/`cloze_item_id` 可以把错误事件追到复习句子，但完成判断还需覆盖“未首答”和“复习计划已删除”两种情况。
- 已发现当前复习生命周期存在两个关键边界：首次直接答对不会进入 7/15 天计划；进入计划后，现有查询又会因“最近一次必须答错”而漏掉后续到期复习。下一步需先确认产品期望的完成定义。
- 用户确认完整三轮后才完成，并确认按单词独立推进。已完成三种技术方向的上下文调查，准备提交推荐架构供确认。
- 用户批准新增单词级进度表方案；已将数据模型、逐词推进、接口口径、历史迁移和测试要求写入设计文档。
- 设计文档已以提交 `9d7500c` 单独提交，未包含工作区内已有实现改动；等待用户最后审阅后进入实施计划。
- 用户要求开始执行；已加载 writing-plans、TDD、planning-with-files 和 executing-plans 规则，开始按现有集中持久化路径编写实施计划。
- 已映射用户端、Java 接口和 Go 管理后台现有错题查询；确定用户端七字段布局可复用，Java/Go 主查询需要切换到单词级进度事实表。
- 已生成七任务 TDD 实施计划，覆盖进度表、状态服务、全部入队入口、逐词三阶段、用户 API、两端页面、历史迁移和非 Docker 全链路验收。
- Task 1 RED 测试已写入：锁定同词 upsert 重置、句子按下标关联，以及立即/7 天/15 天原子推进 SQL 合同。
- Task 1 RED 已按预期因 Mapper 不存在编译失败；已添加实体、原子 Mapper 和幂等建表/历史回填 SQL，准备验证 GREEN。
- Task 1 首次 GREEN 被全量编译环境阻断：旧项目所有 Lombok 生成符号同时缺失，说明是注解处理器未运行而非新增 Mapper 合同失败；已记录并进入工具链根因排查。
- Task 1 GREEN 完成：显式使用项目要求的 JDK 21 后 Mapper 合同 3/3 通过；状态表、索引、幂等历史回填、单词重置/关联/三阶段原子 SQL 已落地。
- Task 2 RED 测试已写入：覆盖标准化、三词独立关联、到期正确词推进、非到期正确词不推进和单个错误词重置。
- Task 2 RED 已按预期因服务不存在编译失败；已实现标准化、幂等句子关联、逐下标推进和完成词结果返回。
- Task 2 GREEN 完成：Mapper + Service 聚焦测试 7/7 通过；补充保护确保旧生成请求不会把已完成单词重新激活。
- Task 3 RED 测试已写入：普通答错必须激活单词进度；首次造句和幂等命中都必须关联三个标准化单词。
- Task 3 RED 已确认只因生产构造器/接入不存在失败；已在普通答题通知前激活状态，并在造句首次保存、冲突回读和幂等命中三条路径关联进度。
- Task 3 GREEN 完成：宿主环境聚焦测试 10/10 通过；普通答题所有集中持久化路径和造句三条保存路径已接入单词进度。
- Task 4 RED 测试已写入：Word Agent 部分错误按下标处理、仅第三阶段完成词写入掌握状态、首次全对不提前掌握，并移除到期查询的最近错误限制。
- Task 4 GREEN 完成：聚焦测试 11/11 通过；Word Agent 题目逐词推进，首次正确只进入 7 天、第二次进入 15 天、第三次完成，任务查询不再要求最近答案错误。
- Task 5 RED 合同已改写：接口必须以未完成进度表为主、同一用户单词只取最近元数据、错误次数来自持久进度，并返回稳定 `progressKey`。
- Task 5 RED 已按预期因 DTO 缺少 `progressKey` 编译失败；已将查询改为进度表主集合，并以统一错误事件 CTE 仅补充每词最近七字段元数据。
- Task 5 GREEN 完成：Mapper/Controller 合同 4/4 通过；原 `/events` 路由兼容保留，但分页总数和每行均按未完成唯一单词计算。
- Task 6 RED 合同已写入：用户端文案/key 必须表达持续待复习，管理后台主错题接口必须使用进度表且页面说明“未完成复习”。
- Task 6 两组 RED 均已确认：Vue 仍使用旧的“可入队记录”文案和 `eventKey`，Go 管理接口仍从普通答题明细聚合；测试准确锁定本轮待改边界。
- Task 6 用户端 GREEN：定向契约 2/2 通过；列表仍保留七列，改用 `progressKey`，并明确未完成复习词持续显示。
- Task 6 管理后台 GREEN：定向合同 2/2 通过；主“用户错题集”查询已改为未完成进度表，普通答题历史仅作为补充明细，造句错题历史页保持独立。
- Task 6 聚焦回归完成：Vue 3 个文件 10/10、管理后台 2 个文件 5/5 通过；此前确认的七列与“你的答案”移除行为均保持。
- Task 6 已提交为 `42d7044`；开始 Task 7 数据库迁移、完整测试、构建和非 Docker 运行态验收。
- Task 7 数据库迁移已连续执行两次且幂等成功；当前进度表共 28 个未完成词，其中 21 个立即到期、7 个等待句子、0 个已完成。
- 管理后台主查询已在真实 PostgreSQL 执行成功：总数 28，首屏返回 20 行；表字段、单词关联、状态过滤与排序均可执行。
- Java 全量测试 71/71 通过；主用户前端全量测试 25/25 及生产构建通过。
- 管理后台全量 74 项中本任务相关用例全部通过，唯一失败仍是修改前已知的模型配置旧合同；Go 首次全量测试仅被默认缓存目录权限拦截，正在用临时缓存重跑。
- Go 全量测试使用任务专用临时缓存后全部通过；管理后台和挖空前端生产构建通过，挖空前端全量测试 19/19 通过。
- 已按完成前验证流程提交独立代码审查，审查范围为 `52265e6..42d7044`，同时继续非 Docker 运行态验收。
- 统一非 Docker 重启成功：CLI Runner、Word Agent、Go、Java、WebSocket 和三个前端的 6011–6018 全部就绪。
- 宿主环境健康检查已通过：Word Agent、CLI Runner、Go API 均返回正常，用户端 `/wrong-words` 返回 HTTP 200。
- 独立代码审查无 Critical，提出 4 个 Important：重复句子关联、重复/乱序错题 upsert、管理端最近来源未合并挖空、迁移 JSON 健壮性；已逐项核实并进入修复。
- 运行日志额外证实空关键字查询存在 PostgreSQL 参数类型推断失败；新增合同测试已完成 RED（2/2 按预期失败）。
- 审查修复已完成定向 RED→GREEN：空关键字 2 项、Mapper 幂等/关联 2 项、迁移 JSON 1 项、管理端统一来源 1 项均先失败后通过。
- 加固迁移已重新应用；真实 PostgreSQL 对非法 JSON、对象 JSON、数组 JSON 分别返回 `[]`、`[]`、原数组，进度数量保持 due=21、waiting_sentence=7。
- 修复后最终回归：Java 72/72、Go 全量、主前端 25/25、管理后台本任务 5/5 均通过；主前端与管理后台生产构建通过。
- 第二轮审查提出的两个 Important 已完成 RED→GREEN：乱序/重复错误事件不会倒退复习阶段，普通答题最近时间使用答题明细时间；修复提交为 `e595fd5`。
- 第三轮独立代码审查确认无 Critical、无 Important，可继续交付。
- 最终非 Docker 重启完成，6011–6018 全部 Ready；Word Agent、CLI Runner、Go API、用户错题集、挖空前端和管理后台健康检查均返回 HTTP 200。
- 最终启动时间段内 Java 与 Go 服务没有新增 ERROR、panic 或 fatal；当前进度表仍为 28 个未完成词，其中 21 个立即到期、7 个等待生成句子。

# 2026-07-30 错题集增加例句与目标词高亮

- 已核对用户错题集、Java DTO/SQL Provider、`word` 和 `word_clean_best_sentence` 数据结构。
- 用户确认采用原词库英文例句优先、AI 最佳例句回退；不展示中文翻译。
- 用户批准在现有接口中一次性关联例句，并在独立“例句”列安全高亮目标词。
- 已写入完整设计文档并完成占位、矛盾、范围和歧义自审；自审明确原始 `word_id` 关联不受词库记录后来停用影响。
- 设计文档已单独提交为 `6dd2e94`，未包含工作区内已有启动脚本和规划文件改动；等待用户复核后再编写实施计划。
- 用户批准开始执行；已按 writing-plans 规则生成四任务 TDD 实施计划，覆盖 Java 例句投影、安全文本高亮、Vue 八列表格和非 Docker 运行态验收。
- Task 1 RED 测试已写入：DTO 必须返回例句与来源，SQL 必须包含原词库优先、AI 最佳回退和 `none` 降级。
- Task 1 RED 已按预期失败：SQL 缺少 `progress.word_id` 例句投影，DTO 反射得到 `NoSuchFieldException`；随后已加入两个响应字段和两级 LATERAL 例句选择。
- Task 1 GREEN：`WrongWordQueueEventSqlProviderTest` 与 `WrongWordControllerContractTest` 共 5/5 通过。
- Task 1 已提交为 `6a3015f`，提交只包含 Java DTO、SQL Provider 和两份聚焦测试。
- Task 2 RED 测试已写入：覆盖大小写保留、词组、全部出现、单词边界、正则字符、无匹配和空句子。
- Task 2 RED 已按预期因 `highlightSentence` 模块不存在失败；已实现纯文本分段函数，全程不解析或注入 HTML。
- Task 2 GREEN：高亮函数 7/7 通过，包含 `raw`/`draw` 子串边界与 `c++` 正则字符用例。
- Task 2 已提交为 `f6de919`，提交只包含高亮纯函数和对应测试。
- Task 3 RED 页面合同已写入：要求八列表头、例句字段、分段高亮、卡片标签，并继续禁止 `v-html`。
- Task 3 RED 已按预期因页面不存在“例句”列失败；现已加入八列布局、安全 `<mark>` 渲染、两行截断和 1279px 卡片断点。
- 页面聚焦测试 17/17 通过；生产类型检查发现标准库将 `RegExpMatchArray.index` 声明为可选，已完成根因确认并加入不改变正常匹配语义的显式守卫。
- 守卫修复后页面聚焦测试再次 17/17 通过，`vue-tsc -b` 与 Vite 生产构建成功。
- Task 3 已提交为 `956ba77`；Task 1-3 生产改动均已提交，开始真实 SQL、全量回归和非 Docker 运行态验收。
- 真实 PostgreSQL 执行生产 SQL 成功：用户 2 返回 22/22 个未完成错词、无重复 `progressKey`，其中原词库例句 20 条、AI 最佳例句回退 2 条、无例句 0 条；关键词过滤保持有效。
- 完整回归通过：Java 73/73，用户前端 32/32，用户前端生产构建成功。Java 首次沙箱运行仅有 4 个既有随机端口权限错误，宿主环境相同命令全部通过。
- 已恢复 PostgreSQL/Redis 基础依赖并用根脚本以宿主机方式启动全部应用；6011–6018 均 Ready，容器列表没有占用这些应用端口的前后端容器。
- 独立浏览器访问 6011 错题集时被重定向到登录页；项目常用本地口令不是应用账号密码，未继续猜测、重置或绕过认证，因此签名 API 和真实页面视觉检查明确保留为未验证项。
- 独立代码审查无 Critical/Important；确认一个小屏卡片 CSS 缺陷。新增合同测试先出现 2 个预期失败，再使用 `example-cell` 外层字段容器和 `example-sentence` 内层截断文本修复，定向测试 3/3。
- 审查修复提交为 `7c29bc1`。修复后新鲜全量验证：Java 73/73、用户前端 33/33、生产构建成功，`git diff --check` 无空白错误。
- 用户将例句来源规则修订为最佳句唯一来源，并明确不回退原词库例句。
- 设计文档已更新并提交为 `ba7563a`；用户复核通过。
- 已生成 `2026-07-30-wrong-word-best-sentence-only.md` TDD 实施计划，范围仅包含 Java SQL 来源选择、测试和完整运行态验证。
- Task 1 TDD 完成：合同测试先因旧字典例句关联按预期失败；删除
  `dictionary_example` 后聚焦测试 5/5，通过提交 `cde0c20`。
- 规格审查确认实现完全符合最佳句唯一来源且无超范围修改。
- 第一轮代码质量审查无生产代码问题，但要求补结果级 PostgreSQL 测试；新增
  `WrongWordQueueEventSqlProviderPostgresTest`，用 7 张临时表直接执行生产
  Provider SQL，实际 PostgreSQL 运行 1/1、聚焦套件 6/6，通过提交
  `bc3de9f`。
- 复审确认 PostgreSQL 行为测试完全解决 Important 测试缺口，无剩余问题。
- 最终全量验证：Java 74/74（PostgreSQL 集成测试实际执行、0 skip）、用户前端
  33/33、生产构建成功。
- 真实用户查询返回 22/22 行：`best_sentence=20`、`none=2`、来源 `word=0`、
  空句 2、重复 `progressKey=0`，关键词无匹配时 total=0。
- 使用 `START_DOCKER_DEPS=0 STOP_DOCKER_STACKS=0` 以宿主机方式重启全部应用；
  6011–6018 均 Ready，所需 HTTP 检查全部 200，Docker 无应用端口映射。
- 新 Java 与 Word Agent 进程启动后无 ERROR/Traceback；唯一错误发生在旧 Java
  关停窗口的 Redis 客户端断连，未延续到新进程。
- 最终整体代码审查结论为 Ready，无 Critical、Important 或 Minor 问题。
- 主代理独立重跑验证：Java 74/74 且 PostgreSQL 集成测试 1/1 实际执行，
  用户前端 33/33、生产构建成功；宿主机再次检查相关页面和健康接口均为
  HTTP 200，Docker 无 6011–6018 应用端口映射。

# 2026-07-30 全部单词答题切换到 word_clean

- 用户要求选择难度和其他所有单词答题入口统一从 `word_clean` 抽题。
- 已启动设计前审计，确认当前段位匹配、指定词库匹配、单人训练和干扰选项均依赖
  `word` 实体；设计批准前不修改生产实现。
- ctx 使用说明接口返回 404，按既有项目约定继续使用源码、表结构和只读数据库核对。

# 2026-08-02 完形句子错题集实现方案

- 已加载 `using-superpowers`、`brainstorming`、`planning-with-files` 和
  `dispatching-parallel-agents` 规则。
- 已记录用户两张截图中的入口、列表参考形态与“任一单词拼错即入集”的核心规则。
- Context Router 当前未暴露 `prepare_task_context` 等工具，按根 `AGENTS.md` 回退到本地
  文档和源码，只读进行设计前审计。
- 已建立本任务阶段；下一步并行核对完形前端、完形后端与现有单词错题复习实现。
- 已分派三个只读检索任务：完形前端、完形后端、现有单词错题集；主代理同步核对跨项目事实文档和历史设计。
- 已确认工作树存在用户的部署与文档改动，后续不改生产文件、不清理现有修改。
- 已完整读取完形前端、Java 后端、主用户前端的项目 AGENTS，以及完形复习和错词掌握两条
  跨项目链路文档；开始核对当前源码与历史设计差异。
- 已读取三份直接相关的历史设计，初步判断应复用现有整句复习状态机与答题界面，只新增持久的
  句子错题事实/列表能力；待源码核实后确定是扩展现有 schedule 还是新增进度表。
- 源码搜索确认 React 已存在“复习计划/错题句子”列表配置，Java 已存在整句 schedule Mapper；
  正在核实它们的实际查询语义、是否可直接升级为用户可见错题集。
- 已确认整题错误判定天然满足“任一空拼错则整句错误”；现有 schedule 完成后删除，成为持久错题集
  的主要建模取舍点。
- 已定位两入口复习状态分裂的根因并形成推荐方向：扩展现有整句 schedule 为持久进度、保留逐词进度，
  到期查询合并去重；正在补齐 UI 字段、接口与迁移/测试边界。
- 三个并行只读审计均已完成；关键源码、状态机、列表契约、管理端参考字段和并发风险已交叉核对。
- 已完成本轮实现方案草案，覆盖三种架构取舍、首页/列表/展开 UI、状态转换、接口、数据库、迁移、
  幂等和测试；待用户确认后再写正式设计文档，不修改生产实现。
- 用户已确认方案并要求开始实现。
- 已写入 `docs/superpowers/specs/2026-08-02-cloze-sentence-wrong-collection-design.md`，自审未发现
  TODO/TBD、空白错误、内部矛盾或未决范围。
- 设计文档已作为唯一文件提交，提交为 `2b46c16`；现有 AGENTS、部署目录和规划文件改动均未暂存。
- 按 brainstorming 的书面规格复核门禁，等待用户确认文档后再调用 writing-plans 并进入 TDD 实施。
- 用户已再次确认开始实现；已加载 writing-plans、executing-plans、TDD 和 worktree 工作流。
- 七任务实施计划已写入并提交为 `bdf409e`，占位符、规格覆盖和类型一致性自审通过。
- worktree 检查发现仓库没有现成目录或 CLAUDE.md 偏好；按必需隔离流程等待用户确认项目内
  `.worktrees/` 位置，尚未修改生产代码。
- 用户明确要求直接在当前 dev 分支开发，已按该授权跳过 worktree。
- Task 1 RED：3 项合同测试按预期以 2 个字段/SQL断言失败和 1 个缺失方法错误失败。
- Task 1 GREEN：持久 schedule、答题幂等字段、实体和原子 Mapper 完成，合同测试 3/3 通过；
  首次 GREEN 的 1 个失败来自合同断言错误匹配 CASE SQL，修正为验证条件分支后通过。
- Task 1 已提交为 `5749a5d`，未包含用户既有部署/文档改动。
- Task 2 完成共享逐空比较、答题流水元数据、submission key 复用和句子/单词双进度更新；
  定向测试 17/17 通过，提交为 `436174d`。
- Task 3 完成句子到期与单词载体到期 CTE 合并去重，并向统计接口增加精确错题/到期数量；
  定向测试 14/14 通过，提交为 `633e707`。
- Task 4 完成错题列表、汇总、分页、详情与最近五次结果 API；合同和服务测试 17/17 通过，
  提交为 `80ff4ec`。
- Task 5 完成首页“错题集 N”入口、前端类型和 API；入口测试 4/4 通过，提交为 `a634360`。
- Task 6 完成独立错题集覆盖层、桌面展开表格、移动卡片、精确计数与客户端幂等提交；
  组件/导航测试 11/11 和生产构建通过，提交为 `add3b19`。
- 后端宿主环境全量测试 90 项：88 通过、0 失败、2 个需显式 PostgreSQL 配置的用例跳过；
  迁移行为测试随后在当前 PostgreSQL 临时 schema 中单独启用并 1/1 通过。
- 完形前端测试脚本已纳入新组件测试，最终 8 个文件、22 项全部通过，TypeScript/Vite 构建通过。
- 两份幂等 DDL 已成功应用到真实 `rob_english_word` 数据库；只读核对确认 4 个答题元数据字段
  和 submission 唯一索引存在，当前句子错题进度 active/completed 均为 0。
- Context Router 工具不可用，按根文档使用受控部署入口；部署预检通过，但共享 Java full 更新
  因缺少明确部署授权被安全审核拒绝，未绕过，前后端运行态验收等待用户授权。
